<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Редактировать отзыв</title>
    <link rel="stylesheet" href="/css/comments.css">
    <link rel="stylesheet" href="/css/footer.css">

</head>
<body>

<main>
    <div class="review-card">
        <h1>Редактировать отзыв</h1>

        <form action="/reviews/{{review.id}}" method="POST" class="comment-form">
            <input type="hidden" name="_method" value="PATCH">

            <label for="name">Имя:</label>
            <input type="text" name="name" id="name" value="{{review.name}}" required>

            <label for="email">Email:</label>
            <input type="email" name="email" id="email" value="{{review.email}}" required>

            <label for="text">Отзыв:</label>
            <textarea name="text" id="text" required>{{review.text}}</textarea>

            <label for="rating">Оценка:</label>
            <input type="number" name="rating" id="rating" value="{{review.rating}}" min="1" max="5" required>

            <label for="productId">ID товара:</label>
            <input type="number" name="productId" id="productId" value="{{review.productId}}" required>

            <label for="userId">ID пользователя:</label>
            <input type="number" name="userId" id="userId" value="{{review.userId}}">

            <button type="submit">Сохранить изменения</button>
        </form>

        <div class="button-group">
            <a href="/reviews/{{review.id}}" class="card-button">Назад</a>
        </div>
    </div>
</main>

</body>
</html>
